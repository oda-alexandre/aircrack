version: "3.7"

services:
  aircrack:
    container_name: aircrack
    image: alexandreoda/aircrack
    privileged: false
    cap_add:
      - NET_ADMIN
    volumes:
      - "${HOME}:/home/aircrack"
    network_mode: host