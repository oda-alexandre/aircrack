version: "3.7"

services:
  aircrack:
    container_name: aircrack
    image: alexandreoda/aircrack
    privileged: false
    volumes:
      - "${HOME}:/home/aircrack"
    cap_add:
      - NET_ADMIN
    network_mode: host
